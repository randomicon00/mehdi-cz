---
title: "Change directory quickly with cd"
date: "2025-09-16"
tags: ["bash", "til"]
draft: false
summary: "cd navigates the working directory; use -, ~ and pushd/popd for convenience."
---

Use cd to change the working directory:

```bash
cd /path/to/dir
```

Placeholder: expand with examples and tips.

Tips:

- `cd -` returns to the previous directory.
- `pushd` / `popd` manage a directory stack.
