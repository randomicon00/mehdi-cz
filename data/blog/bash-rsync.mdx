---
title: "Sync with rsync"

date: "2025-09-16"
tags: ["bash", "til"]
draft: false
summary: "Use rsync for efficient, resumable synchronization; -a preserves attributes."
---

```bash
rsync -avz src/ dest:/backup/
```

Tip: add `--progress` for visual feedback and `--delete` to mirror.
