---
title: "Clear the terminal"

date: "2025-09-16"
tags: ["bash", "til"]
draft: false
summary: "Clear the terminal with `clear` or control-L; use `printf '\033c'` to reset terminal."
---

```bash
clear
# or
printf '\033c'
```

Tip: `reset` is useful if the terminal is in a strange state.
